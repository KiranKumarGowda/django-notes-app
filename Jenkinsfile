pipeline {
    agent { label 'Kiran' }
    stages {
        stage('Test') {
            steps {
                echo "Pipeline is running!"
                echo "pipeline"
            }
        }
    }
}
